#pragma once

// Public facade for the Teensy 4.1 command-server library — the one header a
// sketch includes. Defined by docs/contracts/Teensy_Command_Server_Contract.md
// §5: identity, registration, providers, hooks, start, service.
//
// Intentionally empty at bootstrap (backlog Phase 0). The API surface is
// designed in Phase 1 and must not be finalized before the Library API task
// (docs/companion/Library_API.md) is reviewed.
