# Build and Flash Guide (companion — to be written in Phase 9)

Will document: arduino-cli setup, Teensy 4.1 FQBN (`teensy:avr:teensy41`),
the Teensy core index URL, **pinned tested versions of QNEthernet and
ArduinoJson** (contract §4 requires pinning; an upgrade requires rerunning
conformance), network configuration (static IP vs DHCP), and flashing.

Until written, see the `arduino-cli-builds` skill and `tools/build_teensy.sh`.
