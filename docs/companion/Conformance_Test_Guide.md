# Conformance Test Guide (companion — to be written in Phase 10)

Will document: how to flash the `command_server_conformance` sketch, run
`./tools/run_conformance.sh <board-ip:port>`, interpret case results against
contract §28, and file results into `tests/hardware/results/`.

Until written, see `tests/conformance/README.md` and the
`host-conformance-testing` skill.
